import {computed, observable, action} from 'mobx'

class {{properCase featureName}}Store {
  @observable date = new Date()

  @computed get time() {
    return this.date.toString()
  }
}

export default new {{properCase featureName}}Store()
